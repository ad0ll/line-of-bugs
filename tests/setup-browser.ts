// Browser-environment setup: nothing today. vitest-browser-react's
// render returns its own `screen` object and `expect.element()` provides
// retry-based DOM assertions, so we don't import @testing-library/jest-dom.
