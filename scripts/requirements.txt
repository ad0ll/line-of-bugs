# scripts/requirements.txt — fetcher venv deps only (no ML)
# Set LOB_CONTACT env var to an email upstream APIs can use to reach you.
# Heavy detect_subjects ML deps (torch/transformers/etc.) now live in
# scripts/detect_subjects/requirements.txt — install separately when
# working on the framing detector.
requests>=2.32
Pillow>=10.4
pytest>=8.3
pytest-mock>=3.14
