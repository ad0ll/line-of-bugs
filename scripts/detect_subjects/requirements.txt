# scripts/detect_subjects/requirements.txt — framing-detector ML deps
# Install ON TOP OF scripts/requirements.txt (which already covers
# requests / Pillow / pytest). Kept separate so the lightweight fetcher
# venv doesn't pull ~5 GB of torch + CUDA wheels.
torch>=2.5
torchvision>=0.20
transformers>=4.50
accelerate>=1.0
pyarrow>=18.0
polars>=1.20
opencv-python-headless>=4.10
scikit-image>=0.24
numpy>=2.0
psutil>=5.9
tabpfn>=2.0.0
